# 1️⃣ Base image with JDK
FROM openjdk:17-jdk-slim

# 2️⃣ Set working directory
WORKDIR /app

# 3️⃣ Copy the JAR file from your Maven build
COPY target/*.jar app.jar

# 4️⃣ Run the JAR
ENTRYPOINT ["java", "-jar", "app.jar"]
